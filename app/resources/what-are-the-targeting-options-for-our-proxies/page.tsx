import ResourceArticle from "../../../components/ResourceArticle";

export const metadata = {
  title: "Proxy targeting options | SoksLine",
  description: "Discover geo, network, and session controls available in every SoksLine proxy plan.",
};

export default function Page() {
  return <ResourceArticle slug="what-are-the-targeting-options-for-our-proxies" />;
}
